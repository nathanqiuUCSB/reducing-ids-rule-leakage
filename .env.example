# Optional. Required only for live LLM attacker trials.
# Deterministic mutation generation, Suricata traffic replay, offline reports,
# and the included explorer demo do not need these values.
LITELLM_API_KEY=
LITELLM_BASE_URL=
